{
  "mcpServers": {
    "mcp-agent-mail": {
      "type": "http",
      "url": "http://127.0.0.1:8765/mcp/",
      "core": true,
      "headers": { "Authorization": "Bearer ${HTTP_BEARER_TOKEN}" },
      "options": {
        "timeoutSeconds": 180,
        "initTimeoutSeconds": 30
      }
    }
  },
  "_autoRegistration": {
    "_instructions": "Codex doesn't have built-in SessionStart hooks. Use one of these methods:",
    "method1_wrapper": "Use ./scripts/codex_with_registration.sh instead of bare 'codex' command",
    "method2_alias": "Add to ~/.bashrc: alias codex='./scripts/codex_with_registration.sh'",
    "method3_manual": "Run ./scripts/auto_register_agent.sh --suffix c --program codex-cli --model o3 before starting Codex"
  }
}
